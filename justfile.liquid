build:
    cargo build --release && \
    cargo objcopy --release -- -S -O binary {{project-name}}.vb
assembly:
    cargo rustc --release -- --emit asm --emit llvm-ir
    cargo objdump --release -- --disassemble >{{project-name}}.s